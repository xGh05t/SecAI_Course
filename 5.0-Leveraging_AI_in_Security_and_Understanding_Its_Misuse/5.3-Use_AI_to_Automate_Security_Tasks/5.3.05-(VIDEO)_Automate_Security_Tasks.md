
# 5.3.5 Automate Security Tasks (Video Transcript)

## 1. Introduction to Automating Security Tasks
In today's demonstration, we're going to look at how to
automate various security tasks utilizing an AI system.
I'm going to use the Open AI platform for ChatGPT
to create several different security related scripts, in the Python
language, by feeding previously generated prompts that specifies what type
of task I want it to accomplish.

## 2. Information Gathering Script
In this first example, we have an information gathering about
a public domain.
We are looking for any type of open source intelligence
that we can pull by scanning that domain.
In this case, we're going to utilize the comptia.org domain
as a test domain for our script.
So in this case, I'm going to feed in my
prompt into the ChatGPT system.
And one of the things that I like about ChatGPT
is it provides A generalized overview of what it's going
to provide you.
It also provides tasks that must be accomplished before you
actually execute this script.
In this case, it's going to have different features for
DNS look UPs, reverse DNS look UPs, e-mail enumeration using
search engines.
For example, it's going to look for any e-mail address
that's out on the public web, that's exposed, that ends
in comptia.org, and it's going to provide those e-mail addresses
back to us in our report.
We also see the dependencies.
Like I mentioned before, the dependencies here are anything that
needs to be set up before you would actually execute
the script that ChatGPT is providing. And it walks you
through those steps of what you need to accomplish, whether
you're in Python or in a Windows environment.
There's also iterations for inside PowerShell.

## 3. Executing the Script
If you're on a Mac or a Linux system, it
can really adapt to your environment based on specific information
that you provide the AI system.
Then it finally provides us the actual script.
In this case, the script is called footprinter.py and it
contains all of the source code for that actual script.
Now one of the other things we can utilize ChatGPT
4, or any AI system out there, is not only
to generate scripts like we are doing here, but if
we already have internally generated scripts in any language, we
can feed those as input into the AI system and
have it provide analysis about how we can improve our
code or make modifications on the fly based on given
conditions.
So in this case, it gives us how to use
save the script as footprinter.py, make it an executable by
modifying the permissions, and then simply run it within the
environment specifying what domain you want it to run on.
So what I've done is I've already saved this script
to my local computer and I'm going to execute it
within my environment.
In here, I'm going to use PowerShell with the Python
specification to run this Python script against the comptia.org domain.
Once I hit enter, it'll go through its process and
execute the script.
Once it returns back to the original prompt header, the
script has finished executing.
It has now saved that actual tool in the report
from the script to my desktop system.

## 4. Network Scanning Script
Our next example of a script for development is for
network scanning, looking at various ports that may be open
on the network that could then be used as a
foothold to engage in defensive or offensive operations and security
testing.
So I'm going to go ahead and paste that into
ChatGPT for me. And this I'm going to have it
develop a Python script which will automate network scanning efforts
and find live systems, their open ports, the various running
services along with the service version on a target IP
range of the network that I specify.
Now if I walk into a network and I need
to modify the IP address, I can make changes.
Maybe their IP address is now 192.168.1.0/24. So I can
make modifications to the actual prompt based on the given
conditions.
In this case, I'm specifying that I wanted to use
NMAP to scan all 65,535 ports and I wanted to
export the results as a CSV, or comma separated value
file, like Excel.
Feed that in.
Again it's going to provide you a generalized overview of
what the script is going to do.
It provides you the prerequisites on Linux, on Mac OS,
or on Windows.
IE you must have NMAP installed for this script to
work. And then it provides you the script.
In this case the script is named network_scan.py and it
provides all the source code for that Python script and
then again, how to use it, and the actual output
specifications.
In this case it gives us the actual headers of
each column of information.
In this case, the output fields will include IP addresses,
the host name, protocols and ports and services along with
the product inversion and any other information that it can
provide us.
And we can see an example of what that CSV
file would look like, what that output would look like.
Now, I could also modify this script and say "Based
on the previous output, change the script to PowerShell". And
it's going to immediately look at the output that it
just provided, and it's going to convert that Python script
back into PowerShell,
that can then be run in a Windows environment specifically.
I can also have it convert this script into bash
for Linux environments or any other programming language or scripting
language that I wish to have.
This is how we can utilize AI systems to not
only create new scripts for security tasks, but also edit
existing content for the ever changing landscape of IT and
cyber.

## 5. DNS Information Gathering Script
Our final script demonstration here is going to be gathering
DNS information about a particular domain.
In this case, we're going to again use the comptia.org
domain for DNS open source information, and then again providing
the output as a CSV file.
I'm going to go ahead and let that feed in.
It's going to utilize the dig command on the target
domain, utilizing specific public DNS servers that are available on
the open source Internet.
Provides the overview, then the actual script as it goes
through, and then again it will provide recommendations on how
to execute it, and what the output formats will actually
look like.
But here we see the output of the CSV file,
demonstration of what it would look like in that CSV
file, after we ran the scripting tool. Here it adds
also recommendations.
Would you like to add support for a list of
domains?
Maybe i've got 15 different domains I wanted to scan.
Well, instead of specifying only a single one, I can
actually feed an Excel file or a Word file with
a list of domains that I want it to scan
for.